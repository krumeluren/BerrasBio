@model IEnumerable<BerrasBio.Models.Bookable_Seats>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ticket_Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Show)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Seat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Booking)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ticket_Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Show.ShowID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Seat.SeatID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Booking.BookingID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Bookable_SeatsID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Bookable_SeatsID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Bookable_SeatsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
